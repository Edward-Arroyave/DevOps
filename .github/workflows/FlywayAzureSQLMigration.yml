name: Flyway Migration

on:
  push:
    branches:
      - main

jobs:
  flyway_migration:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flyway
        run: |
          curl -L "https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/10.0.0/flyway-commandline-10.0.0-linux-x64.tar.gz" -o flyway.tar.gz
          tar -xzf flyway.tar.gz
          sudo mv flyway-10.0.0 /opt/flyway
          echo "/opt/flyway" >> $GITHUB_PATH

      - name: Run Flyway Migration
        id: migrate
        run: |
          /opt/flyway/flyway -url=$FLYWAY_URL -user=$FLYWAY_USER -password=$FLYWAY_PASSWORD migrate 2> flyway_error.log || echo "error" > flyway_failed

      - name: Handle Migration Error
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          if [ -f flyway_failed ]; then
            FAILED_SCRIPT=$(grep "Migrating" flyway_error.log | tail -n 1 | awk '{print $NF}')
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            
            BODY="## 🚨 Flyway Migration Error Report  
            **Migration File:** $FAILED_SCRIPT  
            **Error Message:** $(tail -n 5 flyway_error.log)  
            **Event Trigger:** flyway migrate  
            **Commit Hash:** ${{ github.sha }}  
            **Date & Time:** $TIMESTAMP  
            **Git Actor:** ${{ github.actor }}  
            **Logs:** See attached log file."

            gh issue create \
              --title "[${{ github.repository }}] 🚨 Error en migración - ${FAILED_SCRIPT##*/}" \
              --body "$BODY" \
              --assignee "Edward-Arroyave" \
              --label "flyway,bug,migration-error"
          fi

    env:
      FLYWAY_URL: ${{ secrets.FLYWAY_URL }}
      FLYWAY_USER: ${{ secrets.FLYWAY_USER }}
      FLYWAY_PASSWORD: ${{ secrets.FLYWAY_PASSWORD }}
